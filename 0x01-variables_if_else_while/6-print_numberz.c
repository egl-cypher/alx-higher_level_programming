#include <stdio.h>
/**
 * main - first function in the program
 * Return: if success then return 0
 */

int main(void)
{
	int a = 48;

	for (a = 48; a <= 57; a++)
		putchar(a);
	putchar('\n');

	return (0);
}
